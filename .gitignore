# ── 原始文件：含個資，永不進版控 ──
doc/
*.docx
*.doc
*.pdf
*.xlsx

# ── 例外：去識別化素材庫（純 .md，可進版控）──
!reference_md_deidentified/
!reference_md_deidentified/**

# ── 去識別化姓名對照（含人名，絕不進版控/歷史）──
scripts/name_map.local.json

# ── 本機金鑰（wrangler dev 用，絕不進版控）──
.dev.vars
.dev.vars.*
worker/.dev.vars
worker/.dev.vars.*

# ── Cloudflare Worker 本機產物 ──
worker/.wrangler/
worker/node_modules/
node_modules/

# ── 暫存／系統 ──
.DS_Store
__pycache__/
*.pyc
scripts/extract/
_*.json

# 批次抽取暫存
_cards/
