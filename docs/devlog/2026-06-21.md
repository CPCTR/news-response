# 開發日誌 — 2026-06-21

## 專案
台灣中油桃園煉油廠「新聞說明稿快速編輯程式」。結構化表單 → Worker 代理 LLM 生稿 → 人工核稿 → 回填學習。
治理見 `CONSTITUTION.md` / `FRAMEWORK.md` / `MODELS.md`。

## 今日決策（含第一性原則的取捨）
- **去識別化「人名分兩類」**：公開職務首長/民代（部長/董事長/廠長/發言人/立委/市議員）可具名（公開資料）；員工/民眾/傷亡者一律去名。理由：用錯名字比不具名更傷機關觀感，所以名單要可維護、具名前要確認。修了 CONSTITUTION 條目六與 CLAUDE.md。
- **參考語料庫仍維持全去名**：`reference_md_deidentified/` 是 RAG 素材，與「對外稿可具名現任首長」分流處理。
- **歷史零殘留**：人名也在 init commit，故 squash 全部 commit 成單一乾淨 commit + gc，歷史/檔名/註解/report 全清。
- **姓名對照表不入庫**：`scripts/name_map.local.json` 含人名，gitignored；committed 腳本只留載入器。
- **出稿模型走免費但要好**：GitHub Models 由 gpt-4o 升 **gpt-5-chat**（免費、品質大升、drop-in）；保留 Anthropic 為 fallback。
- **草稿要「拿來就能用」**：來源/待確認標記不得汙染對外本文，改列「【內部審查註記（請勿對外）】」獨立區。

## 今日產出（皆在 repo 內）
- **去識別化收尾**：`reference_md_deidentified/` 正文 38 檔去名、3 檔改名；清 40+ 人名與員工 CPC email；全歷史 squash 成單一乾淨 commit，殘留人名 = 0。
- **長官具名功能**：`officials.json`（公開資料、build 內嵌）+「🗂 長官名單」編輯器（含最後確認日、複數副總/副執行長、立委/市議員）；生成前「具名確認」面板（預設正確直接 Enter、逾 180 天標⚠、○○○ 佔位不具名）；確認姓名注入 prompt 具名規則。
- **語音預校正**：`corrections.json`（錯字→正確詞對照，build 內嵌）+「📒 校正詞庫」編輯器 + 「🎤 語音輸入」(Web Speech API, zh-TW) + 生成前自動套用。
- **Worker**：`GH_MODEL=openai/gpt-5-chat`；worker.js 自動依模型挑 `max_tokens`/`max_completion_tokens`（支援 gpt-5/o3/o4 推理版）。
- **出稿格式**：本文乾淨無 (來源:)/[待確認]，溯源改列內部審查區；複製/LINE/學習只取對外本文（`publicDraft`）。
- 治理：CONSTITUTION 條目六、CLAUDE.md 改兩類分流。

## 驗證
- 去識別化：全歷史 `grep` 人名 0 命中；單位名稱保留。
- 功能：build 成功、JS `node --check` 通過、過期/佔位/具名/校正純邏輯單元測試全過。
- 端到端：本機 wrangler dev + GitHub PAT 跑通真實生成（`provider: github_models`, `gpt-5-chat`）；實測新格式本文 0 標記、具名正確、成因自動降為「調查中」。

## 待辦 / 下一步（明天）
- [ ] 把現任官員姓名填進 `officials.json`（公開資料）並 commit；目前為 ○○○ 佔位。
- [ ] #2 表單一致性檢查：勾選不相容組合（如 廢水↔攔油索/回收油料）時生成前提醒。
- [ ] 上線部署：`wrangler deploy` + `wrangler secret put GITHUB_TOKEN`；Worker 加 `x-app-key` + 鎖 `ALLOWED_ORIGIN` 防白嫖（PLAN Phase 2）。
- [ ] 術語修正分頁接 `glossary.json`（PLAN Phase 3）。
